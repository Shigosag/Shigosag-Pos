{
  "name": "shigosag-pos-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npx prisma db push --schema=backend/prisma/schema.prisma --accept-data-loss && npx prisma generate --schema=backend/prisma/schema.prisma && npx tsx backend/src/server.ts"
  }
}
