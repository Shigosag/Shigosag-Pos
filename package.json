{
  "name": "shigosag-pos-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "npm run start --workspace=backend",
    "build": "npm run build --workspace=backend",
    "install-all": "npm install"
  }
}
